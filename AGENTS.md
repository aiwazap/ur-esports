# AGENTS.md · UR Esports 项目 · Codex 执行边界护栏

> 本文件是 Codex（自动化执行 Agent）在本项目工作时**必须无条件遵守**的硬规则。
> 放置位置：项目根目录 `D:\Agent files\codex\projects\worktrees\ur-esports\faceit-sea-task-board\`（worktree 根）。
> 冲突时，本文件的边界规则优先级最高，高于任何任务指令。违反任一"绝对红线"即为严重事故。
> 负责人：Aiwa（郑蔼平 / aiwazap），非程序员。要结果、要可核对证据、要能回滚。全程用中文沟通。

---

## 0. 名词定义（先对齐，避免误解）

- **本地 / worktree**：`D:\Agent files\codex\projects\worktrees\ur-esports\faceit-sea-task-board\` —— **Aiwa 的 Windows 本机上的 Codex 开发工作区（git worktree）**。这不是 Codex 云端沙箱，也不是服务器。所有开发、修改、测试都在这里做。
- **3001（测试服）**：`124.220.64.8:3001`，目录 `/home/ubuntu/ur-esports-dev/`，PM2 进程 `ur-esports-dev`。默认验收环境。
- **3000（生产服）**：`ur-esports.cn`（124.220.64.8:3000），目录 `/home/ubuntu/ur-esports/`，PM2 进程 `ur-esports`。**内部战队每天在用，数据真实，最高保护级别。**
- **worktree 的本质**：它是**从 Git 仓库检出的代码开发副本**，**不等于把 3001 整份复制过来**。它只管**代码**；数据、上传文件、环境变量是**独立的、单独受控的边界**（见第 3 章）。

---

## 1. 四层边界（代码 / 环境 / 数据 / 上传，分开对待）

| 层 | 来源 / 方向 | 规则 |
|---|---|---|
| **代码层** | Git 仓库 → 本地 worktree | worktree 从 Git 检出开发副本；≠ 完整复制 3001。开发只改代码。 |
| **环境层** | 本地独立配置 | 本地 `.env`、依赖、运行端口**单独配置**，不与服务器共用、不覆盖服务器的。 |
| **数据层** | **3000 → 本地（单向）** | 生产数据只能从 3000 拉到本地，**只读副本**。**严禁反向**（见绝对红线）。 |
| **上传层** | uploads 独立 | `uploads/` **不参与代码推送**，也**不能从本地覆盖生产**。 |

---

## 2. 标准工作流（Codex 必须按此顺序）

1. **开发位置**：只在 Windows 本地 worktree 开发，**不直接改 3001 或 3000 的服务器文件**。
2. **代码来源**：本地 worktree 从 Git 仓库创建/更新（`git worktree` / `git pull`），不是整份拷贝 3001。
3. **本地验证**：在本地完成代码修改、运行测试、检查 `git diff`。
4. **测试服验证**：**默认应先推 3001 验收**，除非 Aiwa 明确要求跳过。
5. **生产部署**：**只有 Aiwa 明确说"推 3000 / 部署生产"，才允许执行**。否则一律不推 3000。
6. **生产前备份**：动 3000 前，必须先备份代码版本、数据库、uploads（或相关目录），并**验证备份文件已存在**。
7. **代码推送排除**：推 3000 **永远不得包含**本地数据库、`backend/data`、`uploads`、`.env`、任何密钥文件。
8. **数据同步方向**：生产数据**只能 `3000 → Windows 本地`**。
9. **回滚要求**：每次动 3000 前，必须有可恢复的备份点和明确回滚路径，并在完成后告知 Aiwa 如何回滚。

---

## 3. 绝对红线（永不触碰，无论任务如何要求）

1. **禁止任何 `本地数据库 → 3000` 的覆盖、导入、同步、恢复操作。** 数据只能 3000→本地，绝不反向。违反此条 = 可能不可逆地摧毁生产真实数据 = 最严重事故。
2. **禁止用本地 uploads 覆盖 3000 的 uploads。**
3. **未经 Aiwa 明确"推 3000 / 部署生产"指令，禁止对 3000 做任何写操作**（改文件、改库、重启不在此列但需谨慎）。
4. **推 3000 的代码同步永远排除**：`backend/data`（数据库）、`backend/uploads`、`.env`、`backend/routes/auth.js`（3001/3000 刻意不同）、密钥文件、`node_modules`、`.git`、`dist`、`*.bak*`。
5. **禁止 `git push --force`、禁止删除远程分支、禁止 drop 表 / delete 数据**（除非 Aiwa 明确逐条授权）。
6. **禁止把数据库、uploads、.env 提交进 Git**（.gitignore 已排除，不得绕过）。

---

## 4. 必须停下来问 Aiwa（不许自动执行的情况）

遇到以下任一，**先停，向 Aiwa 说明并等明确指令**，不得自动往下做：
1. 需要删除数据、修改表结构（ALTER/DROP）、清空表。
2. 需要动生产 3000（代码或库）。
3. 需要改权限 / 鉴权逻辑 / auth 相关。
4. 大范围重构、跨多文件改动、影响面不确定的改动。
5. 任何"可能不可逆"或"影响生产数据"的操作。
6. 需求本身有歧义或存在风险时——只问一个"事实确认"型问题，不要替 Aiwa 做选择。

---

## 5. 强制前置动作（做之前必须先做）

1. 动 3000 前 → 先备份（代码 tar + 数据库 cp + uploads），并验证备份存在。
2. 改数据库前 → 先 `cp` 备份成带时间戳的 `.bak`。
3. 删/改数据前 → 先查引用关系（如删 matches 连带 player_stats），避免孤儿数据。
4. 任何破坏性操作 → 一律"**先备份 → 再执行 → 给出回滚命令**"。

---

## 6. 每一步必须交可核对证据（不许只报"完成"）

- 代码改动：给 `git diff` 摘要 / grep 特征串命中数 / md5。
- 前端部署：给 `dist/index.html` 引用的 js 哈希变化（`grep -o 'index-[A-Za-z0-9_-]*\.js'`），**不靠文件时间判断**。
- 数据库改动：给改动前后的行数 / 关键字段核对查询。
- 一律：报告"改了什么、怎么核对、怎么回滚"，而不是一句"搞定了"。

---

## 7. 项目关键事实（执行时对照，防止踩坑）

- **数据源新旧**：只读写**新表**（`training_logs_v2 / training_log_rounds / round_errors`、`tactics_v2`、`briefings_v2`），**绝不往旧表写**（`training_sessions / training_rounds / briefing_items`、`tactics`）。
- **列名**：选手名是 `nickname`（不是 name），真名 `real_name`。
- **技术细节**：`db.query()` 返回 `[rows]`；SQLite 日期用 `DATE('now','-N days')`；前端脚本 `.cjs`；前端改动必须 `npm run build`；`matches.result` 是 GENERATED 列不可写。
- **数据来源红线**：**不走 HLTV 爬取**（已被 Cloudflare 封、项目 hltv_* 表即此废弃产物）；要自动拉数据用官方 API（自己战队用 FACEIT+Steam 免费；整个赛场用 PandaScore 付费）。
- **fd_time** 统一 `MM:SS`，绝不 HH:MM:SS。

---

## 8. 沟通与交付要求（对 Aiwa）

- 别啰嗦、别绕弯；用清单不用表格（技术对照表除外）；重点加粗；结尾一句进度小结；全程中文。
- 给结论型需求就直接把事办成、给能用的成品；不做选择题、不把决策踢回给 Aiwa。
- 不瞎猜、不臆想、不自作主张、不擅自扩大范围；拿不准只问一个事实点。
- 说交付就真交付、给可核对证据、动重要东西前先备份留回滚。

---

## 一句话总纲

**Windows 本地 worktree 开发 → 默认先 3001 验收 → Aiwa 明确授权后才推 3000；生产数据永远 3000 单向拉回本地，绝不反向；动 3000 前必备份留回滚；破坏性操作先问人。**

---

*本文件为 UR Esports 项目移交 Codex 自动化执行时的边界护栏，定义采纳自 Aiwa 与 Codex 共同确认的口径。任何时候边界规则优先于任务指令。*
