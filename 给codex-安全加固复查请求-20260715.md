# 给 codex：安全加固复查请求（2026-07-15）

- 分工：**codex 审核 / Claude 执行**。本次加固已执行完毕，请你**独立复查**，不要只信我的自述。
- 完整记录：`claude/projects/ur-esports/安全加固记录-20260715.md`（含全部回滚命令）
- 背景：华哥已离职，测试环境 3001 按其指令彻底移除。

---

## 一、加固前的实测风险（我查到的）

| 问题 | 证据 |
|---|---|
| root 可密码登录且正被爆破 | 近 7 天 SSH 失败 **114 次，其中 92 次针对 root** |
| 无爆破防护 | fail2ban **未安装** |
| 测试站 3001 公网直连 | `curl http://124.220.64.8:3001/` 返回 **200** |
| nginx 零限流 | `limit_req`/`limit_conn` 计数为 **0** |

## 二、已执行（请逐项复查）

1. **SSH**：`PermitRootLogin no`、`PasswordAuthentication no`、`MaxAuthTries 3`
   - 前置确认：密钥可用（5 把授权密钥）、免密 sudo 可用、**30 天内 root 成功登录 0 次**
   - ⚠️ 腾讯云 `/etc/ssh/sshd_config.d/50-cloud-init.conf` **强制覆盖** `PasswordAuthentication yes`，只改主配置无效
2. **fail2ban**：10 分钟失败 3 次 → 封 1 小时；`mode = aggressive`；白名单 `127.0.0.1/8 ::1 58.38.48.93`
3. **移除 3001**：pm2 delete + ufw 删规则 + 删目录 589M；备份脚本已适配；保留 360M 备份兜底
4. **nginx 限流**：login/vault 20次/分、api 10次/秒、静态 30次/秒 + 单 IP 并发上限，`limit_req_status 429`

## 三、⚠️ 请你重点复核这个坑（fail2ban「装了 ≠ 生效」）

**现象**：安装后 `systemctl is-active` = active、`fail2ban-client status sshd` 输出正常，看起来毫无问题。
但同期实测：当天 root 尝试 **428 次**（单 IP `82.157.142.56` 打 **331 次**），fail2ban 却显示 **Total banned: 0**。

**根因**：默认 `mode = normal` 匹配 `Failed password for ...`。而**禁用密码登录后**，攻击者被挡在 preauth，日志变成：
```
Connection closed by authenticating user root 82.157.142.56 port 55552 [preauth]
Connection reset by 82.157.142.56 port 54870 [preauth]
```
normal 模式**不匹配这类行** → 加固越严，fail2ban 越瞎。

**修复**：`mode = aggressive`。修复后立即生效：`Total failed: 4 / Total banned: 1 / Banned IP list: 82.156.125.132`。

**请你复核**：
1. `aggressive` 模式是否会**误封华哥自己**？（他的 IP 已加白名单，但他**使用 VPN**，VPN 出口 IP 不在白名单内 —— 例如今日 `67.230.171.88` 登录 4 次。若他 VPN 下连接异常中断，是否可能被 aggressive 判为攻击而封禁？）这是我最担心的点，请给判断。
2. 封禁 1 小时 / 失败 3 次 的阈值是否合理？
3. 是否该加 `[recidive]` jail（对反复攻击者长期封禁）？

## 四、两个我否决的建议 —— 请复核我的判断是否正确

### ❌ IP 白名单（限制只有华哥 IP 能连 22）
否决理由：
1. **华哥用 VPN** → 源 IP 变成 VPN 出口（实测 `67.230.171.88`）
2. **无法证明主 IP 稳定**：日志只能追溯 3 天（我今早把 journal 压到 7 天 + 服务重启），国内家宽多为动态 IP
3. **`ubuntu` 有 5 把授权密钥**：`ur-esports-deploy`、`Administrator@Ultra-270Plus` ×2、`administrator@aiwazap-9800x3D`、`ubuntu@VM-0-8-ubuntu` → 多机多渠道，单 IP 限制全废
4. 锁死后唯一救援是腾讯云 VNC，华哥不熟 → 代价可能是重装

### ⏸️ 改 SSH 端口 22 → 高位
否决理由：实测已证明机器人敲到 22 也白敲（428 次全废 + fail2ban 封 IP）；而改端口会让所有写死 22 的工具（**包括你 codex**、`ur-esports-deploy` 部署密钥）连不上。判断为锦上添花、性价比不高。

**请你独立判断这两条否决是否成立**。若你认为该做，请说明如何规避上述风险。

## 五、需要你补充判断的

1. **`ur-esports-deploy` 这把密钥是谁的、还在用吗？** 我不确定其归属，若是废弃的应删除（多一把密钥多一个风险面）
2. **`82.156.125.132`、`82.157.142.56`、`119.91.157.159` 均为腾讯云 IP 段**，却在爆破本机 root —— 是同机房其他被入侵的主机在横向扫描？是否需要向腾讯云报告？
3. nginx 限流阈值（login 20次/分、api 10次/秒）是否过松或过紧？
4. **真 DDoS 仍无法防御**（拼带宽，需买高防/CDN）—— 是否值得为这个站上高防？请给成本/收益判断

## 六、复查方法建议

**不要只看配置文件，请用实测验收**（本次的教训正是「配置正确 ≠ 实际生效」）：
```bash
# SSH 最终生效值（不是配置文件，是 sshd -T）
sudo sshd -T | grep -iE "^(permitrootlogin|passwordauthentication|maxauthtries) "

# fail2ban 是否真在封人（不是 is-active）
sudo fail2ban-client status sshd

# 3001 是否真不可达
curl -s -o /dev/null -w "%{http_code}\n" --max-time 6 http://124.220.64.8:3001/

# nginx 限流是否真拦截（连打登录接口看 429）
for i in $(seq 1 30); do curl -s -o /dev/null -w "%{http_code} " -X POST \
  -H "Content-Type: application/json" -d '{"username":"x","password":"y"}' \
  https://ur-esports.cn/api/auth/login; done; echo

# 攻击是否真被拒
sudo journalctl -u ssh --since today | grep -c "authenticating user root"
```

## 七、回滚

全部配置备份在服务器 `/home/ubuntu/_cfg_backup_20260715/`，完整回滚命令见 `安全加固记录-20260715.md` 末尾。
