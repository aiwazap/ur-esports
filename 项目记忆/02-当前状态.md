# 当前状态

## 2026-07-16 Claude 侧并行工作树 + Codex 视频脚本目录建立

- **背景**：华哥要求"不同页面需求各建本地工作树以便并行开发"，以及给 Codex 一个固定的视频脚本产出目录。
- **前置动作**：`feature/faceit-sea-task-board` 分支上原有大量未提交改动（FACEIT SEA 页面/后端路由、首页 Hero 视频素材、vault/private 私有页面、运营文档等 67 个文件）已提交，commit `b7a5e75`，作为新工作树的公共基线（决策：新工作树要看到这些已有工作，而非从干净 main 分支开）。
- **新建 3 个 Git 工作树**（均位于 `D:\Agent files\claude\projects\worktrees\ur-esports\`，从 `b7a5e75` 分叉，各自独立分支）：

  | 工作树目录 | 分支 | 后端端口 | 前端端口 |
  |---|---|---|---|
  | `首页\` | `feature/homepage` | 3002 | 5175 |
  | `赛训汇总报告\` | `feature/training-report` | 3003 | 5176 |
  | `用户权限\` | `feature/user-permissions` | 3004 | 5177 |

  - 主仓库（本目录，`D:\Agent files\claude\projects\ur-esports`）继续用分支 `feature/faceit-sea-task-board`，端口不变：后端 3001 / 前端 5173。
  - 每个新工作树已从主仓库复制 `backend/.env` 并改好对应 `PORT`；`frontend/vite.config.js` 的 `apiProxy` 默认值和 `server.port` 已同步改成表中端口，互不冲突。
  - 后续新需求按"一个页面/功能域 = 一个工作树"的方式追加，目录名用中文功能名，分支名用英文 `feature/xxx`，端口在此表基础上依次 +1 分配（下一个可用：后端 3005 / 前端 5178）。
  - 工作树的 `.env` 里的 JWT_SECRET/FACEIT_API_KEY 与主仓库一致（同一份复制），非独立密钥；如需要各自隔离需另行处理。

- **新建 Codex 视频脚本目录**：`D:\Agent files\claude\projects\UR-视频脚本\`（项目仓库外部，不进 git，不与网站代码混放），下设四个平台子目录：`抖音\` `哔哩哔哩\` `视频号\` `小红书\`。约定：Codex 只在此目录内产出脚本/计划/提醒，不接触代码仓库；涉及网站落地的部分（如首页 Hero 视频）由 Claude 按脚本内容在网站工作树里执行/编译/写代码。

- **待办**：三个新工作树尚未 `npm install`，也未启动过服务，首次使用前需要各自装依赖。

- 更新时间：2026-07-12 18:54（Asia/Shanghai）
- 主仓库：`D:\Agent files\codex\projects\ur-esports`，分支 `main`，HEAD `85f06f0`。
- FACEIT SEA 工作树：`D:\Agent files\codex\projects\worktrees\ur-esports\faceit-sea-task-board`，分支 `feature/faceit-sea-task-board`；已重建有效 Git worktree 指针并逐文件回填校验。
- C 盘主目录、工作树、日志和本地备份已按复制与 SHA-256 校验后删除；Documents 主目录快照、工作树差异归档及修复前完整副本均已保留在 D 盘项目目录/`D:\Agent files\_rollback`。
- 工作树与此前 D 盘镜像存在 6 个差异文件（SQLite 数据库、两个构建文件、两个首页源码文件）；镜像已独立归档，未覆盖当前工作树。
- 本地 3011/5174 服务已为迁移停止；后续仅允许从上述 D 盘工作树启动。
- 当前 Git 工作区存在既有未提交改动和构建缓存；本轮未提交、未部署、未访问 3001/3000。

## 下一步

1. 从 D 盘工作树启动本地服务并复验 3011/5174。
2. 清理或明确保留构建缓存后，再按任务完成独立提交。
3. 所有生产操作继续遵循 3000 → 本地单向数据规则。

## 2026-07-13 Claude 副本同步 FACEIT SEA 演示版（Claude 侧记录）

- 华哥反馈 Claude 副本页面与 codex 本地演示样式不符。排查结论：**复制本身无误**（与主仓库 main 工作区字节一致），但 codex 演示用的是 faceit-sea-task-board worktree 的**未提交工作区** + 数据库（1.1MB，含生产拉取数据），主仓库 main 的库是空壳。
- 已将 worktree 全部未提交改动（Overview 改版、FaceitSea 新页面、faceit-sea 后端路由、gsap 依赖、AGENTS.md、players.js、docs 需求文档等 19 个文件）同步到 Claude 副本，并检出同名分支 `feature/faceit-sea-task-board`（复制副本 .git 中残留的指向 codex worktree 的无效注册已清理）。
- 数据库：空壳库已备份为 `ur_esports.db.bak_empty_before_sync_20260713`，替换为 worktree 的数据库副本（admin 可登录，24 选手/84 比赛）。
- 与 codex 版唯一有意差异：`frontend/vite.config.js` 代理默认端口 3011 → 3001（Claude 侧后端跑 3001，符合测试走 3001 红线；可用环境变量 `VITE_API_PROXY` 覆盖）。
- `backend/.env`（不入 Git，复制时会丢失）：PORT=3001、本地随机 JWT_SECRET、从 worktree 复制的 FACEIT_API_KEY。
- 注意：直接访问 http://localhost:3001（旧 dist 静态页）会因 CORS 白名单无 localhost:3001 而登录失败，本地一律用 http://localhost:5173。

## 2026-07-13 推生产记录（华哥明确下令"推上3000"，Claude 执行）

- 内容：FACEIT SEA 演示版（本地当前状态）→ 生产 ur-esports.cn:3000。方式：本地 npm run build 后直传文件（frontend/dist 整包替换 + backend/server.js + 新增 backend/routes/faceit-sea.js + .env 追加 FACEIT_API_KEY），**未走 git**。
- 数据红线遵守：生产数据库/uploads 未动；部署前三重备份在服务器 `*.bak_before_faceit_deploy_20260713`（db/server.js/dist）。
- 验证：首页 200 且加载新 bundle（index-BiMgYEtl.js）、faceit-sea 路由 401 正常、pm2 error log 干净。
- 临时演示文件 ur-hero-demo.html 已移出 public，未进生产包。
- ⚠️ **给 codex 的重要提醒**：生产现在运行的代码不在任何 git 提交里（源头是 faceit-sea-task-board worktree 的未提交状态 + Claude 侧 Hero 残影修复）。下次若跑 deploy.sh（git pull + 重建）会把网站**回退**。必须尽快：worktree 变更提交到分支 → 合并 main → 推 GitHub，让 git 与线上对齐。
- 回滚方法：ssh 后把三个 .bak_before_faceit_deploy_20260713 备份复制回原位并 pm2 restart ur-esports。

## 2026-07-15 服务器磁盘清理（100% → 64%）

- 起因：系统盘 40G 用满，可用仅 360M，随时会让部署/备份静默失败。codex 调研提出清理范围，Claude 复核后执行（数字全部属实）。
- 释放约 13.5G：Chrome 崩溃转储 7.8G（34,583 文件）、backups_7day 旧包 3.5G、backups 历史包 1.5G、apt/日志/pip+pnpm 缓存 715M。**现可用 14G**。
- 受保护资产 MD5 逐项校验未变：生产库、.env、uploads(27)、dist(85)、`prod3000_20260715_030001.tar.gz`（最新生产备份，保留）。
- 保守调整两处：`/root/.cache/ms-playwright`(625M) **未删**（是 Playwright 浏览器本体，非缓存，openclaw 在用）；codex 部署备份保留最新 2 份。
- ✅ **两个根因已于同日修复**（codex 出方案、Claude 执行验证，详见下节）
- 🕳️ **踩坑记录**：`sudo rm -rf /root/xxx/*` 在 ubuntu 账号下**静默失败**——通配符由无权读 /root 的当前 shell 展开为空，sudo 收到空参数、返回码 0 假装成功。必须用 `sudo sh -c '...'` 或 `sudo find -delete`。删后要复测体积，不能只看返回码。

## 2026-07-15 晚 关闭游客访问 → 网站转为「仅授权账号可访问」

- **起因**：华哥认为网站"外部访问不到、已绝对私密"。**实测证伪**：匿名请求调 `/api/auth/guest` 拿令牌即可读到选手数据（200）。**游客入口=对全世界敞开的只读门**；fail2ban/限流防的是"撬锁"，不防"从敞开正门走进来"（游客是合法进入，不触发任何防护）。华哥选方案 A：删除。
- **执行**：删后端 `POST /guest`；`middleware/auth.js` 由「guest 只读」改为**直接拒绝一切 guest 令牌**（含历史令牌）；前端删游客按钮 + 顺带删「立即注册」入口（后端本就 403）；`admin` 弱密码 `admin123` → `aZAP@1211`（郑蔼平已是该密码，未动）。改前备份数据库 `ur_esports.db.bak_before_pwchange_20260715`。
- 🕳️ **中途自查纠正**：曾把历史令牌兜底写进 `routes/auth.js` 的 `router.use`——**只管 `/api/auth/*`，历史令牌照样能打 `/api/players`**。真正必经关卡是 `middleware/auth.js`，已移正。
- 🕳️ **测试方法教训**：首次用**本地** `.env` 的 JWT_SECRET 伪造历史游客令牌 → 401（签名无效），**测试无意义**。本地与生产 JWT_SECRET 不同，必须**上服务器用生产密钥签发**才是真实验证。
- **验证（匿名攻击面）**：游客接口 404 ✅ / 无凭证读数据 401 ✅ / 注册 403 ✅ / **真实历史游客令牌读写均 403** ✅。构建包中 `游客访问`/`auth/guest`/`立即注册` 命中数均 0。授权账号正常：admin 200（读到 24 名选手）、郑蔼平 200。
- **私密性边界（诚实）**：数据私密 ✅；但**站点存在性仍公开**——`https://ur-esports.cn` 登录页对公网返回 200，任何人能看到"有这么个系统"，只是进不去。要连存在性都藏起来需整站 IP 白名单，但华哥用 VPN 会自锁，不建议。

## 2026-07-15 安全加固 + 移除测试环境 3001

- **背景**：华哥已离职，不再需要测试环境；安全体检发现真实攻击痕迹。
- **实测风险**：root 可密码登录且近 7 天被爆破 **92 次**（总失败 114 次）；**未装 fail2ban**（可无限试）；测试站 **3001 公网直连返回 200**；nginx **零限流**。
- **已加固**（全部实测验证）：
  1. SSH：禁 root 登录（30天内 root 成功登录 0 次，确认无人用）、禁密码登录（仅密钥）、MaxAuthTries 3。⚠️**关键坑**：腾讯云 `/etc/ssh/sshd_config.d/50-cloud-init.conf` 会**强制覆盖** `PasswordAuthentication yes`，只改主配置无效；以 `sshd -T` 为准。改完用 reload + **新连接验证成功**才算数。
  2. fail2ban：10 分钟失败 3 次 → 封 1 小时；白名单含华哥 IP `58.38.48.93` 防误封。
  3. **移除 3001**：pm2 delete + ufw 删规则（实测外网 000 不可达）+ 删目录 589M；备份脚本已适配不再备份 test3001。保险：`backups_7day/test3001/*.tar.gz`(360M) **保留未删**。
  4. nginx 限流抗 CC：login/vault 20次/分、api 10次/秒、静态 30次/秒 + 并发上限。**实测连打 30 次登录 → 25 次被 429 拦截**，正常使用不受影响（登录/游客/读数据均 200）。
- 🕳️ **重大坑：fail2ban「装了 ≠ 生效」**。安装后 `is-active`=active、status 输出正常，看着毫无问题，但实测当天 **root 尝试 428 次（单IP 82.157.142.56 打331次）、fail2ban 却 Total banned: 0，一个没封**。根因：默认 `mode=normal` 只匹配 `Failed password`，而**禁用密码登录后攻击者被挡在 preauth**，日志变成 `Connection closed by authenticating user root ... [preauth]`，normal 模式不匹配 → **加固越严 fail2ban 越瞎**。修复：`mode = aggressive`，立刻抓到并封禁 `82.156.125.132`。**教训：安全组件必须用「真实攻击是否被拦」验收，`is-active` 只证明进程在跑、不证明在干活。**
- **实战数据**（加固当天真有人在打）：root 尝试被拒 **428 次**、密码尝试被拒 **47 次**、fail2ban 已封禁攻击者。
- ❌ **放弃 IP 白名单**：华哥**使用 VPN**（源 IP 变成 VPN 出口，实测 67.230.171.88）；日志仅能追溯 3 天无法证明主 IP 稳定；ubuntu 有 **5 把授权密钥**（多机多渠道）；锁死后只能靠腾讯云 VNC 救援而华哥不熟 → 做了必锁门。
- ⏸️ **不改 SSH 端口**：实测机器人敲 22 也白敲，改端口会让写死 22 的工具（含 codex、ur-esports-deploy 密钥）连不上，性价比低。
- **仍无法防御真 DDoS**（拼带宽，唯一解是买高防/CDN）。CC 已防住。
- 已请 codex 复查：`给codex-安全加固复查请求-20260715.md`。**codex 复查结论 CHANGES_REQUIRED，3 项收尾已全部完成**（回执 `给codex-复查整改完成回执-20260715.md`）：
  1. 🕳️ **codex 抓到我漏掉的真问题**：`/etc/nginx/sites-enabled/` 里有我 7/14 留下的 `ur-esports.bak_before_vault_20260714`，与主配置**声明相同 server_name** → `nginx -t` 报 4 条 conflicting server name。当前因主文件先加载限流仍生效，但**文件排序/改名就会让旧配置抢先 → 限流与 vault 静默失效**。**我当时只看了 `syntax is ok`，没看警告**。已移出至 `_cfg_backup_20260715/nginx/`，冲突 4→0。
  2. **fail2ban maxretry 3→5**：aggressive 模式下华哥 VPN 抖动（`Connection reset ... [preauth]`）10 分钟内累计 3 次可能误封，而 VPN 出口不在白名单。已禁 root/密码，放宽不引入风险。误封自救：`sudo fail2ban-client set sshd unbanip <IP>`。
  3. **密钥去重**：codex 更正——不是「5 把」而是 **5 行/4 把唯一密钥**，`Administrator@Ultra-270Plus` 有完全重复行（正是 codex 与 Claude 在用的那把）。已去重 5→4 行并**用新连接验证登录成功**。`ur-esports-deploy` 核实后（近 3 天登录 **0 次**、服务器无脚本引用、本机无对应私钥）**经华哥确认已撤销**：4 行 → **3 行**，被撤销的公钥单独存档 `revoked_ur-esports-deploy.pub` 可原样加回，撤销后连续两次新连接验证成功。**最终 3 把密钥**：`ubuntu@VM-0-8-ubuntu`（服务器自用）、`Administrator@Ultra-270Plus`（Claude+codex 共用，含华哥 VPN 出口）、`administrator@aiwazap-9800x3D`（华哥另一台机器）。
- 🕳️ **测试副作用（重要）**：压测登录接口 12 次会把**应用层** `loginLimiter`（15分钟10次、**按 IP 计**）配额烧光 → **同一出口 IP 下华哥也会被锁 15 分钟**。今后压测按 IP 限流的接口须避开华哥使用时段或提前告知。
- codex 其余判断均采纳：不做 IP 白名单 ✓、不改 SSH 端口 ✓、暂不启用 recidive（先跑 7 天）、腾讯云来源 IP **不能定性为横向入侵**（只能确认在扫描）、nginx 阈值不再收紧、暂不买高防。
- 磁盘：65% → **63%**。文档：`安全加固记录-20260715.md`（含完整回滚命令）。
- 建议后续（未执行）：改 SSH 端口、ufw 限制 SSH 来源 IP、上 CDN。

## 2026-07-15 冗余清理 + 空间全景（codex 审计 / Claude 执行）

- codex 出只读审计 `codex/projects/ur-esports/docs/服务器与项目冗余审计-20260715.md`，Claude 复核后执行第一批。
- **执行结果**：`backups` 536M→262M；生产 `backend/data` 从 18 个文件→**只剩主库 1 个**（17 份历史库副本 15M 已归档）；删 `dist.bak_before_guest`(41M)、6/5 全量目录(199M)、07-08 测试 tar(92M)。归档去处 `/home/ubuntu/backups/db-archive-20260715/`（20 文件/16M），**零数据损失**。核对：主库/.env MD5 未变、uploads 27、dist 85、vault 在、PM2×2 online、Chromium active。
- ⚠️ **codex 报告有 1 处误判（已回执纠正）**：「脚本同一分钟生成两份归档」**不是 bug**，是 Claude 手工连跑两次验证轮转所致（05:02:12/05:02:49，MD5 相同；cron 仅 1 条；`KEEP=2` 本就是 codex 定的策略）。**按其建议改脚本会把对的改坏**，明天 cron 一跑自愈。
- ⚠️ **规避一处数据损失**：codex 建议直接"淘汰 6/5 旧全量"，但该目录内含**日常备份（仅滚动 2 天）覆盖不到的 6/5 数据库**。已先抽出存为 `ur_esports_20260605_from_fullbackup.db` 再删目录。
- **空间全景**（回答"UR 才几百兆为何占几十 G"）：见 `服务器空间使用说明-20260715.md`。25G 已用中——交换文件 **10.1G**（`/swapfile` 8G **使用量 0**、`/swap.img` 用 479M）、`/usr` 系统 5.0G、`/root` AI 工具链 5.3G（pnpm 2.3G/openclaw 1.8G/ms-playwright 625M 浏览器本体）、ext4 保留块 1.7G，**全部与 UR 无关**。**UR 生产站仅 365M，不可替代数据只有 75M（库 1.1M + 照片 74M）**，其余为可重建的 node_modules/dist。删光整个网站也只腾出 0.9%。
- 回执文档：`给codex-审计报告纠正与执行回执-20260715.md`。待办：/swapfile 是否缩减（需授权）、测试服 uploads 285M（需查引用）、旧表/HLTV 技术债（独立立项）。

## 2026-07-15 服务器根因修复（codex 出方案 / Claude 执行）

**Chrome 崩溃循环（已止）**
- 根因：两套 lighthouse-chromium 服务同时 enabled 抢 9222（root 用户级指向不存在的 chromium-1217 → 203/EXEC）；系统级 SIGTRAP + `Restart=always/RestartSec=3` 无限重启，累计 **659,502 次**，每 33 秒刷一个 dmp。
- **隔离实测定性**：用 9223 + 全新用户目录跑 chromium-1208，2 分钟全稳、零 dmp → **二进制无问题，是 `browser-existing-session` 目录损坏**。
- 处置：停用并 disable root 用户级服务；旧会话目录备份后换干净目录；系统级加 `StartLimitIntervalSec=300`/`StartLimitBurst=3`/`Restart=on-failure`/`RestartSec=60`，启动参数加 `--disable-crash-reporter --disable-breakpad`。
- **实测确认 Chrome 不会自行生成 DevToolsActivePort**（干净目录全盘找不到），故保留 `update-devtools-port.sh`，但等待 30s→10s，并传入 `$MAINPID` 使主进程一退出即失败。
- 验收：连续 **10 分钟** active、NRestarts=0、9222 持续 OK、**dmp 新增 0**、openclaw 在线、PM2 双进程 online。

**backup_7day.sh（已重写）**
- 新策略：prod3000 每天 / test3001 仅周日，各留 2 份；`.partial` → `tar -tzf` 校验 → 原子 mv，失败即删（杜绝伪装成功的坏包）；可用<5G 跳过并告警；清理按 **mtime**（find -printf）不解析文件名；未知文件只告警不删。
- 目录改为 `backups_7day/prod3000/` 与 `backups_7day/test3001/`。cron 未动（每日 03:00，周日逻辑在脚本内）。
- 验收：shellcheck **零问题**；真实备份成功；恢复列表校验 uploads 27 文件**逐一比对一致**、db/.env/vault/dist 均在包内、node_modules 已排除；轮转实测删最早留 2 份；总占用 739M（稳定态约 1.1G，目标 ≤1.5G）。

**回滚**：`服务器修复回滚手册-20260715.md`；配置备份在服务器 `/home/ubuntu/_cfg_backup_20260715/`（含 22M 原 Chrome 会话目录，内有原登录态）。

## 2026-07-14 全量备份 + 整体说明书

- 生产全量备份：`D:\Agent files\_rollback\ur-esports-prod-full-20260714.tar.gz`（187MB，463 项：代码+数据库+uploads+.env+dist+nginx配置+pm2快照+crontab，排除 node_modules/venv/.git）
- 《项目整体说明书》落地：`docs\项目整体说明书.md`（架构拓扑/四路数据源/设计逻辑/华哥设计偏好/部署与回滚/已知待办），任何新会话凭此可接手项目
- 隐藏叙事页《第 N 天》设计定稿（评审 45/50 胜出）：天数计数器贯穿全页 + 分段时间密度映射 + 史官三律处理"不公" + 示人/本人双模式，方案细节在会话工作流产物中，待华哥确认后实施
