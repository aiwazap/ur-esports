# 历史事件流水账

## 2026-07-11 本地数据库为何为空排查
- 问题：Aiwa 询问本地部署是否应来自 3001 备份，为什么页面显示空数据。
- 核验：
  - 当前主 worktree DB 只有 348,160 bytes，核心业务表全为 0。
  - 当前 FACEIT SEA worktree DB 只有 393,216 bytes，核心业务表全为 0，仅有 FACEIT 新表初始化数据。
  - 3001 测试服 DB 为 1,015,808 bytes，含 `players=24`、`matches=77`、`training_logs_v2=25`、`training_log_rounds=1158` 等真实测试数据。
  - Windows 备份目录存在一份与 3001 同体量、同计数的 DB：`C:\Users\Administrator\Documents\AgentFiles_Migration_Backup_20260710_160607\ur-esports-dev\backend\data\ur_esports.db`。
- 结论：3001 数据备份存在，但未被恢复到当前活跃 worktree；当前本地服务使用的是各 worktree 自己的空业务库。

## 2026-07-11 FACEIT worktree 恢复 3001 数据并验收
- 操作目标：让 FACEIT SEA 看板在本地真实 3001 数据环境下验收，而不是空业务库。
- 已备份当前 FACEIT worktree DB：
  - `C:\Users\Administrator\Documents\ur-esports-worktrees\faceit-sea-task-board\backend\data\ur_esports.db.bak_before_3001_restore_20260711_011502`
  - `C:\Users\Administrator\Documents\ur-esports-worktrees\faceit-sea-task-board\backend\data\ur_esports.db.bak_before_3001_restore_20260711_011520`
- 已将 3001 Windows 备份库恢复到 FACEIT worktree：
  - 来源：`C:\Users\Administrator\Documents\AgentFiles_Migration_Backup_20260710_160607\ur-esports-dev\backend\data\ur_esports.db`
  - 目标：`C:\Users\Administrator\Documents\ur-esports-worktrees\faceit-sea-task-board\backend\data\ur_esports.db`
- 已启动本地后端 `3011`，补齐本地 `.env` 的 `PORT=3011` 和 `JWT_SECRET`。
- 已触发 `/api/faceit-sea/summary?date=2026-07-09`，自动创建并初始化 FACEIT 新表。
- 核验结果：
  - 真实业务数据：`users=10`、`players=24`、`matches=77`、`player_stats=744`、`training_logs_v2=25`、`training_log_rounds=1158`、`round_errors=860`、`tactics_v2=138`、`briefings_v2=24`
  - FACEIT 数据：`faceit_sea_config=8`、`faceit_players=5`、`faceit_matches=2`、`faceit_match_players=10`
  - 构建通过，产物：`index-D9YvKwfI.js`、`index-Dcgl-bdY.css`
  - 浏览器宽屏验收：`/overview` body 横向溢出 `0`，FACEIT 首页卡 `400x323`，队员区横向溢出 `0`；`/faceit-sea` 横向溢出 `0`，队员槽 `5`，最近 room `2`。
- 追加修复：
  - 真实数据验收发现首页旧 KPI 只统计 `scrim` 训练赛，导致地图胜率已有 5 场正式赛时，顶部“已完成比赛”和地图标题仍显示 0。
  - 已在 `frontend/src/pages/Overview.jsx` 做前端兜底：训练赛 KPI 为 0 但地图统计有真实比赛时，用 `mapStats` 和 `recentOfficial` 补齐已完成比赛、胜率、回合胜率。
  - 修复后浏览器验收：`已完成比赛=5`、`胜率=60%`、`回合胜率=36.6%`、地图标题 `近14天 · 5 场`，FACEIT 首页卡仍为 `400x323` 且无溢出。
  - 最新构建产物：`index-BLn-4O8E.js`、`index-Dcgl-bdY.css`。

## 2026-07-11 同步 3000 生产数据到本地 FACEIT worktree
- 触发原因：Aiwa 发现 3001 备份中训练赛记录不完整，要求同步一份 3000 数据到本地。
- 执行边界：仅执行 `3000 → Windows 本地`，未向 3000 写入任何文件、数据库或 uploads。
- 生产源库只读核验：
  - `/home/ubuntu/ur-esports/backend/data/ur_esports.db`
  - `users=11`、`players=24`、`matches=84`、`player_stats=776`
  - `training_logs_v2=30`、`training_log_rounds=1408`、`round_errors=925`
  - `tactics_v2=139`、`briefings_v2=26`
  - 训练日志范围：`2026-05-25 ~ 2026-07-10`
- 本地替换前已备份：
  - `C:\Users\Administrator\Documents\ur-esports-worktrees\faceit-sea-task-board\backend\data\ur_esports.db.bak_before_3000_sync_20260711_014327`
- 已拉取生产库副本：
  - `C:\Users\Administrator\Documents\ur-esports-worktrees\faceit-sea-task-board\backend\data\ur_esports_3000_pull_20260711_014327.db`
- 已替换本地目标库并重启 3011，随后触发 FACEIT summary 自动补回 FACEIT 新表。
- 已同步生产 uploads 到本地：
  - 生产 uploads：`74M`、`27` 个文件
  - 本地 uploads：`27` 个文件、`77,403,645` bytes
  - 本地旧 uploads 备份：`C:\Users\Administrator\Documents\ur-esports-worktrees\faceit-sea-task-board-local-backups\uploads_before_3000_sync_20260711_014658`
- 验收：
  - `/api/training-plans/logs` 返回 `45` 条可显示记录，最新 `2026-07-10 dy2k`。
  - `/api/dashboard/overview?start=2026-06-28&end=2026-07-11` 返回近 14 天训练赛 `6` 场、胜率 `33.3%`。
  - `npm run build` 通过，产物仍为 `index-BLn-4O8E.js`、`index-Dcgl-bdY.css`。

## 2026-07-11 FACEIT SEA 页面预览版重塑
- 触发原因：Aiwa 反馈首页 FACEIT SEA 任务卡过分拥挤，并询问是否需要 image2/Figma/专业设计技能。
- 执行判断：这是数据后台界面，不是视觉概念图；直接在真实本地页面中改 React/CSS，比 image2/Figma 更适合交付和验收。
- 改动：
  - `frontend/src/components/FaceitSeaSummaryCard.jsx`：compact 模式改为首页 mini 摘要卡，移除首页 5 人网格。
  - `frontend/src/pages/FaceitSea.jsx`：记录页改为任务驾驶舱布局，包含目标进度、今日压力、五人状态、Match room 复核、最近完整五排、提醒节点。
  - `frontend/src/pages/faceit-sea.css`：新增 mini 卡、驾驶舱、队员状态、房间复核、右侧任务栏等样式和响应式规则。
- 验收：
  - `/overview`：FACEIT mini 卡 `400×322`，横向溢出 `0`，首页卡内不再渲染 `.faceit-player-grid`。
  - `/faceit-sea`：横向溢出 `0`，任务驾驶舱 `1488×262`，`squadCards=5`，`latestRooms=2`。
  - `npm run build` 通过，产物：`index-CsI8gHm9.js`、`index-DfosCWVj.css`。

## 2026-07-11 首页即将开始赛事宽度调整
- 触发原因：Aiwa 要求缩短首页“即将开始赛事”的宽度，并询问 FACEIT “距离目标 218”的含义。
- 修改：`.ov8-toprow` 的第一列从 `560px` 改为 `440px`，第三列 FACEIT 仍为 `400px`，释放空间给中间“近14天数据速览”。
- 验收：宽屏 1920×1030 下，“即将开始赛事”宽 `440px`，“近14天数据速览”宽 `552px`，FACEIT mini 卡宽 `400px`，页面横向溢出 `0`。
- 解释口径：“距离目标 218” = `3200 - 2982`，即目标 ELO 减去当前配置队员中的最高 ELO。
- 构建产物：`index-vuTwFREN.js`、`index-DJ0L4v6_.css`。

## 2026-07-11 首页即将开始赛事内容精简并扩 FACEIT
- 触发原因：Aiwa 澄清需求是“即将开始赛事”内部只显示赛事名称和开始日期，并把缩短的空间留给 FACEIT SEA。
- 修改：
  - `frontend/src/pages/Overview.jsx`：即将开始赛事行仅渲染赛事名称与 `match_date`。
  - `frontend/src/pages/overview-v8.css`：顶部三列改为 `320px minmax(0, 1fr) 520px`。
  - `frontend/src/components/FaceitSeaSummaryCard.jsx`：首页 FACEIT 口径文案改为“距 3,200 ELO 还差 218”。
- 验收：宽屏下“即将开始赛事”宽 `320px`，FACEIT 首页卡宽 `520px`，横向溢出 `0`。
- 构建产物：`index-uKn8PEaq.js`、`index-Bzwjkb50.css`。

## 2026-07-11 FACEIT SEA 从最高分改为四人平均
- 触发原因：Aiwa 指出 `azura 小号` 的 `2982` 是错误口径，并明确 Azura 即将离队；当前只应计入 `HZ / Coach`、`4ever`、`glong`、`melody` 四人，目标是四人平均 ELO 到 `3200`。
- 数据来源说明：Azura 的 `2982` 来自本地样本初始化数据 `sample:-maider666`，不是 FACEIT API 实时匹配结果。
- 本地库修改前已备份：
  - `C:\Users\Administrator\Documents\ur-esports-worktrees\faceit-sea-task-board\backend\data\ur_esports.db.bak_before_faceit_roster_avg_20260711_042508`
- 本地库修改：
  - `azura 小号 / -maider666` 已置为 `is_active=0`。
  - 当前四人平均：`(2865 + 2798 + 2760 + 2824) / 4 = 2811.75`，接口按 1 位小数返回 `2811.8`。
  - 距离 `3200`：`388.2`。
  - 按近期单胜平均 `24.5` 分估算，还需净胜 `16` 场。
- 代码修改：
  - `backend/routes/faceit-sea.js`：默认阵容移除 Azura，旧库自动停用 Azura，summary 增加四人平均字段，近战斗记录上限改为 5。
  - `backend/config/schema.sql`：初始化不再插入 Azura，并补停用 Azura 的兼容更新。
  - `frontend/src/components/FaceitSeaSummaryCard.jsx`：首页 FACEIT 卡拆成“四人平均目标”和“近5场记录”两个内框。
  - `frontend/src/pages/FaceitSea.jsx`：详情页全部改为四人平均口径。
  - `frontend/src/pages/faceit-sea.css`：压缩首页 FACEIT 卡高度，避免与中间卡形成明显落差。
- 验收：
  - 接口：`roster_size=4`、`current_average_elo=2811.8`、`remaining_average_elo=388.2`、`required_net_wins=16`、`has_azura=False`。
  - 首页：FACEIT 卡 `520×221`，与近14天速览同高；横向溢出 `0`；近5场当前显示 `2026-07-09 13:5`、`2026-07-09 13:9`。
  - 详情页：四人状态卡 `4` 张，无 Azura，近5场区域只显示日期和比分。
  - 构建产物：`index-BIckfvqd.js`、`index-DSGUIwdx.css`。

## 2026-07-11 首页头图机甲龙/UR LOGO 融合修复
- 触发原因：Aiwa 反馈首页头图右侧机甲龙 + UR LOGO 更像一张带底色 JPG 贴在头图上，而不是与底色融为一体。
- 排查：
  - 当前首页使用 `frontend/public/reshape/home/hero.png`。
  - 原实现为 `.ov8-hero-art img` 普通图片 + 径向 mask；由于图片本身含完整矩形深蓝背景，左侧边界仍会露出贴片感。
- 修改：
  - `frontend/src/pages/overview-v8.css`：为 `.ov8-hero` 增加低透明全局背景图层。
  - `frontend/src/pages/overview-v8.css`：为 `.ov8-hero-art::after` 增加主视觉伪元素图层，使用 `mix-blend-mode: lighten`。
  - `frontend/src/pages/overview-v8.css`：原 `.ov8-hero-art img` 改为透明占位，避免图片矩形边缘参与渲染。
  - `frontend/src/pages/overview-v8.css`：主视觉层增加线性渐隐 mask，左侧缓慢融入页面底色。
- 验收：
  - 浏览器 `/overview`：hero 展开高度 `411px`，页面横向溢出 `0`。
  - CSS 状态：主视觉伪元素 `artPseudoBg=true`，混合模式 `lighten`。
  - 构建通过，产物：`index-B7Vnr_N6.js`、`index-CLW4GTzU.css`。

## 2026-07-11 首页头图融合二次细调
- 触发原因：第一次融合后右侧图片仍能看出深蓝矩形底，需要继续压低 JPG 底色参与度。
- 修改：
  - `frontend/src/pages/overview-v8.css`：降低 `.ov8-hero::before` 背景图层透明度并收窄显示区域。
  - `frontend/src/pages/overview-v8.css`：将 `.ov8-hero-art::after` 从 `lighten` 改为 `screen`，同时提高对比、压低亮度、增强左侧渐隐 mask。
  - `frontend/src/pages/overview-v8.css`：调整发光层范围，避免整块暗底被照亮。
- 验收：
  - 浏览器 `/overview`：hero 展开高度 `411px`，hero 内容高度 `364px`，页面横向溢出 `0`。
  - CSS 状态：主视觉伪元素 `artAfterBlend=screen`、`artAfterOpacity=0.96`。
  - 构建通过，产物：`index-DK64EqJI.js`、`index-CG6WJ9s8.css`。

## 2026-07-11 首页顶部 FACEIT SEA 再布局
- 触发原因：Aiwa 提出 4 条修正：头图需要清晰底板；近14天速览与 FACEIT SEA 互换且缩短；FACEIT SEA 加宽；近5场记录补时间；平均 ELO 下罗列四人个人分数。
- 修改：
  - `frontend/src/pages/Overview.jsx`：取消头图 3 秒自动收起，默认常驻展开；顶部三卡顺序改为“即将开始赛事 / FACEIT SEA / 近14天数据速览”。
  - `frontend/src/pages/overview-v8.css`：头图背景加亮并加入网格底板；顶部三卡改为 `320px / 760px / 316px` 口径；近14天速览压缩样式。
  - `frontend/src/components/FaceitSeaSummaryCard.jsx`：首页 compact 卡新增个人 ELO 区，按 `4ever / glong / melody / HZ` 排序；近5场记录改为 `日期 - 时间 - 比分`。
  - `frontend/src/pages/faceit-sea.css`：FACEIT compact 卡改为三栏：平均 ELO / 个人 ELO / 近5场记录。
- 验收：
  - `/overview`：头图高度 `411px`，内容高度 `364px`，页面横向溢出 `0`。
  - 顶部可见卡宽：即将开始赛事 `320px`，FACEIT SEA `756px`，近14天数据速览 `316px`。
  - FACEIT 个人 ELO：`4ever 2798`、`glong 2760`、`melody 2824`、`HZ 2865`。
  - 近5场当前记录：`07-09 - 21:47 - 13:5`、`07-09 - 21:11 - 13:9`。
  - 构建通过，产物：`index-BDKTuEy1.js`、`index-B5xh55qn.css`。

## 2026-07-11 FACEIT SEA KPI 口径纠正
- 触发原因：Aiwa 核对 FACEIT 页面后指出三名队员分数与展示不符，且 HZ 是教练，没有 FACEIT SEA KPI。
- 修改：
  - `backend/routes/faceit-sea.js`：默认 KPI 队员改为 `4ever / L1XYyy=2403`、`glong / NDWSS=2452`、`melody / Me1odyovo=1910`；summary 与同步队员接口排除教练。
  - `backend/config/schema.sql`：初始化种子同步改为三名队员真实 ELO，并将 `HZ / HZZZZZ` 固定为 `is_active=0 role=Coach`。
  - `frontend/src/pages/FaceitSea.jsx`、`frontend/src/components/FaceitSeaSummaryCard.jsx`：页面文案从“四人/HZ”改为动态队员口径；API 状态文案改成“FACEIT API 未配置”。
  - 本地数据库 `faceit_players` 已同步修正；操作前备份为 `backend\data\ur_esports.db.bak_before_faceit_kpi_roster_fix_20260711_192441`。
- 验收：
  - 本地 DB：`kpi_count=3`，HZ 为 `is_active=0 role=Coach`。
  - `/api/faceit-sea/summary?date=2026-07-11`：`roster_size=3`、`current_average_elo=2255`、`remaining_average_elo=945`、队员为 `4ever/L1XYyy=2403, glong/NDWSS=2452, melody/Me1odyovo=1910`。
  - 前端构建通过，产物：`index-B5x4fYnP.js`。

## 2026-07-11 配置 FACEIT Developer API Key
- 触发原因：Aiwa 授权“用我的 Chrome 去配置 FACEIT API Key”，用于自动同步 FACEIT SEA 队员公开 ELO。
- 操作：
  - 使用 Chrome 已登录的 FACEIT Developer Portal，进入 `RVN Esports Stats` App 的 `API Keys` 页面。
  - 页面显示已有 `Server side` Key，且提示一人只能有一个 API Key。
  - 读取该 Key 后写入本地 FACEIT SEA worktree 的 `backend\.env`，未在对话中输出 Key 内容。
  - 写入前备份 `.env`：`backend\.env.bak_before_faceit_api_key_20260711114636`。
- 验收：
  - FACEIT Data API 测试 `NDWSS` 返回 `200`，`itemCount=1`。
  - 本地同步接口 `POST /api/faceit-sea/sync-players` 返回 `sync_success=true`。
  - 同步结果：`L1XYyy=2403`、`NDWSS=2452`、`Me1odyovo=1910`。
  - summary 返回 `api_key_configured=true`、`roster_size=3`、`current_average_elo=2255`。

## 2026-07-11 FACEIT 看板近 5 场字段与布局修正
- 触发原因：Aiwa 提出 FACEIT 看板 4 条问题：只展示 3 名有效队员；近五场记录要有日期、时间、比分、地图、属性；首页卡未来不能被 5 场记录撑高；右上角“未开始”含义不清。
- 操作范围：仅修改 FACEIT SEA 本地 worktree 代码，未触碰 3001/3000。
- 修改：
  - `backend/routes/faceit-sea.js`：给 FACEIT match 增加 `match_type_key/match_type_label`，普通 FACEIT SEA 归类为 `排位`，ESEA/League/Open/Tournament 等归类为 `联赛`；`not_started` 文案改成 `当日无记录`。
  - `frontend/src/components/FaceitSeaSummaryCard.jsx`：首页近 5 场记录补齐 `日期 / 时间 / 比分 / 地图 / 属性`。
  - `frontend/src/pages/FaceitSea.jsx`：详情页 Match room 与右侧近 5 场同步展示地图和排位/联赛属性。
  - `frontend/src/pages/faceit-sea.css`：队员展示改三列；首页 FACEIT 卡固定高度，近 5 场改为卡内横向记录带，避免撑高。
- 改动前备份：`C:\Users\Administrator\Documents\ur-esports-worktrees\faceit-sea-task-board-local-backups\faceit_dashboard_before_latest_records_20260711_195421`
- 验证：
  - `npm run build` 通过，产物 `index-DBpj_Pfc.js`、`index-BSnuDHwV.css`。
  - `node --check backend\routes\faceit-sea.js` 通过。
  - 3011 本地接口返回 `roster_size=3`、`status_text=当日无记录`、近 5 场样本含 `Anubis 排位` 与 `Dust 2 排位`。
- 收尾：已关闭临时 `3311` 验证服务，并重启本地 `3011` 后端到新代码。

## 2026-07-11 首页 FACEIT 卡重排修复
- 触发原因：Aiwa 截图反馈首页 FACEIT 卡高度被压缩，“近5场记录”标题掉到底部，实际记录不可见。
- 操作范围：仅改 FACEIT SEA 本地 worktree 前端样式，未触碰后端数据、3001 或 3000。
- 修改：
  - `frontend/src/pages/overview-v8.css`：首页顶部区域改为两行布局，FACEIT SEA 独占第一整行；“即将开始赛事”和“近14天数据速览”放到第二行，数据速览宽屏横向 4 格。
  - `frontend/src/pages/faceit-sea.css`：首页 FACEIT compact 卡改为横向三块：平均 ELO / 个人 ELO / 近5场记录；近5场记录直接展开为小卡，不再被固定高度裁切。
- 改动前备份：`C:\Users\Administrator\Documents\ur-esports-worktrees\faceit-sea-task-board-local-backups\faceit_home_relayout_20260711_201020`
- 验证：`npm run build` 通过，产物 `index-Gi0m9yzq.js`、`index-BAslzKmp.css`。

## 2026-07-11 FACEIT 战绩同步与今日记录
- 触发原因：Aiwa 要求增加“同步 FACEIT 战绩”，个人 ELO 旁显示本月场次并按场次排序，近五场改为今日场次记录，同时 FACEIT 模块改为黑橙色。
- 操作范围：仅改 FACEIT SEA 本地 worktree 代码与本地 SQLite 测试库，未触碰 3001 或 3000。
- 修改：
  - `backend/routes/faceit-sea.js`：新增 `POST /api/faceit-sea/sync-matches`，从 FACEIT Open API 拉取战绩与 stats，写入 `faceit_matches` / `faceit_match_players`；summary 增加本月场次统计并按场次降序。
  - `frontend/src/components/FaceitSeaSummaryCard.jsx`：首页卡片改为“今日场次记录”，个人 ELO 旁展示本月场次。
  - `frontend/src/pages/FaceitSea.jsx`：新增“同步 FACEIT 战绩”按钮，详情页记录改为今日场次。
  - `frontend/src/pages/faceit-sea.css`：今日记录改为每场一行，并追加 FACEIT 黑橙主题覆盖。
- 改动前备份：`C:\Users\Administrator\Documents\ur-esports-worktrees\faceit-sea-task-board-local-backups\faceit_sync_matches_20260711_201738`
- 验证：
  - 已重启本地后端 `3011`，新进程 PID `45236`。
  - `POST /api/faceit-sea/sync-matches` 返回 `success=true`、`synced=30`、`skipped=0`、`errors=[]`。
  - `GET /api/faceit-sea/summary?date=2026-07-11` 返回 `data_mode=live`、今日 2 场：`Dust 2 13:6 排位`、`Mirage 13:2 排位`。
  - 本月场次排序返回：`melody 30场`、`glong 23场`、`4ever 16场`。
  - DB 计数：`faceit_matches=30`、`api_matches=30`、`today_api_matches=2`、`faceit_match_players=73`。
  - `node --check backend\routes\faceit-sea.js` 和 `npm run build` 通过，产物 `index-DZECoM8v.js`、`index-DshQ7cME.css`。

## 2026-07-11 FACEIT 同步入口与首页看板可读性修正
- 触发原因：Aiwa 反馈看不到同步 FACEIT 数据入口，并指出首页 FACEIT SEA 看板字体偏小、个人 ELO 列不齐、今日场次记录区域留白过多。
- 操作范围：仅改 FACEIT SEA 本地 worktree 前端代码，未触碰 3001 或 3000。
- 修改：
  - `frontend/src/components/FaceitSeaSummaryCard.jsx`：首页 FACEIT SEA 标题区新增 `同步 FACEIT 战绩` 主按钮；个人 ELO 改成 `ID / ELO / 场次` 三列表格式。
  - `frontend/src/pages/Overview.jsx`：首页同步按钮接入 `POST /api/faceit-sea/sync-matches`，同步完成后刷新 FACEIT 卡数据。
  - `frontend/src/pages/FaceitSea.jsx`：详情页同步按钮加主按钮样式。
  - `frontend/src/pages/faceit-sea.css`：放大 FACEIT 字体；平均 ELO 与个人 ELO 框体加宽；桌面三列调整为 `390 / 390 / 594`；个人 ELO 数字右对齐；今日记录行收窄到约 `564px`。
- 改动前备份：`C:\Users\Administrator\Documents\ur-esports-worktrees\faceit-sea-task-board-local-backups\faceit_ui_sync_button_typography_20260711_2036`
- 验证：
  - `npm run build` 通过，产物 `index-E0FlTGCN.js`、`index-BAiuV_gd.css`。
  - 1920×1030 浏览器核对：同步按钮 1 个且可点击；点击后进入 `同步中`，完成后恢复；页面横向溢出 `0`。
  - 首页 FACEIT 卡尺寸 `1432×320`，三块框体 `390×222 / 390×222 / 594×222`。

## 2026-07-11 FACEIT 原生今日比赛行与个人 ELO 间距优化
- 触发原因：Aiwa 要求重新平衡个人 ELO 的 `ID / ELO / 场次` 字段间距，并参照 FACEIT Match history 重做今日比赛记录。
- 操作范围：仅改 FACEIT SEA 本地 worktree 的两个前端文件，未触碰本地数据库、3001 或 3000。
- 修改：
  - `frontend/src/components/FaceitSeaSummaryCard.jsx`：增加地图名到现有 Logo 的映射；比赛行加入表头、W/L/D 标记、日期时间叠排、赛事标签和地图 Logo。
  - `frontend/src/pages/faceit-sea.css`：个人 ELO 改为均衡三列并设 `18px` 列间距；比赛记录改为 FACEIT 深色表格行、胜负色条和固定高度滚动区；补齐 1366 与移动宽度规则。
- 改动前备份：`C:\Users\Administrator\Documents\ur-esports-worktrees\faceit-sea-task-board-local-backups\faceit_native_match_rows_20260711_204431`
- 验证：
  - `npm run build` 通过，产物 `index-CwVdKLE1.js`、`index-BwtvVbSm.css`。
  - 浏览器实测 `1920×1030 / 1366×768 / 430×932` 三档宽度均无横向溢出。
  - Dust 2 与 Mirage 地图 Logo 均加载成功；今日两条记录分别显示 `15:32 13:6 排位 Dust 2`、`15:06 13:2 排位 Mirage`。

## 2026-07-12 01:12 FACEIT 首页个人 ELO 四列排序
- 执行目录：`C:\Users\Administrator\Documents\ur-esports-worktrees\faceit-sea-task-board`
- 需求：个人 ELO 增加 `今日场次`，原 `场次` 改为 `本月场次`，`ELO / 今日场次 / 本月场次` 均支持点击升降序。
- 改动前备份：`C:\Users\Administrator\Documents\ur-esports-worktrees\faceit-sea-task-board-local-backups\faceit_roster_sorting_20260712_011233`
- 修改文件：`backend\routes\faceit-sea.js`、`frontend\src\components\FaceitSeaSummaryCard.jsx`、`frontend\src\pages\faceit-sea.css`。
- 验证：`node --check` 通过，`npm run build` 通过；浏览器 1920/1366/430 三档视口无横向溢出，错误日志为空。

## 2026-07-12 01:21 首页三大模式看板重排
- 执行目录：`C:\Users\Administrator\Documents\ur-esports-worktrees\faceit-sea-task-board`
- 需求：FACEIT 字段居中；FACEIT 小指标改为 `X 场 / X 个胜场`；首页三块模式为正式赛、FACEIT SEA、训练赛；近14天速览进入头图；失误类型与地图胜率同排。
- 改动前备份：`C:\Users\Administrator\Documents\ur-esports-worktrees\faceit-sea-task-board-local-backups\overview_three_mode_board_20260712_012158`
- 修改文件：`frontend\src\pages\Overview.jsx`、`frontend\src\pages\overview-v8.css`、`frontend\src\components\FaceitSeaSummaryCard.jsx`、`frontend\src\pages\faceit-sea.css`。
- 验证：`npm run build` 通过；浏览器 1920/1366/430 三档视口无横向溢出；FACEIT 四列居中；头图展开速览可见；错误日志为空。
## 2026-07-12 01:xx 首页附件版 Dashboard 重塑
- 在 `faceit-sea-task-board` worktree 完成首页布局重塑：Hero、首排三卡、FACEIT SEA、训练三卡、训练重点+地图胜率、底部 6+6 入口。
- 未修改后端和数据库；使用本地前端 fallback 占位补齐 `0Z / drace`、训练个人数据、失误分布和 6 张地图胜率。
- `npm run build` 通过；Playwright + 本机 Chrome 验证 `1920x1030 / 1366x768 / 430x932` 横向溢出均为 `0`。

## 2026-07-12 11:46 首页 Hero 与 FACEIT 裁切回归修复
- 用户反馈：头图无动效、首次不会在 5 秒后收起、手动展开后的规则不正确，FACEIT SEA 与目标图不符且右栏数据消失。
- 已确认截图 `5174` 对应 `faceit-sea-task-board` worktree；改动前完整备份 4 个前端文件到 `overview_hero_faceit_fix_20260712_114624`。
- 修复 Hero 首次会话标记、一次性 5 秒计时、手动交互取消计时、收起高度冲突和入场/呼吸动效；修复 FACEIT 三列 grid-area 与固定高度裁切。
- 实测首次自动收起完成高度 `47px`，手动展开等待 `5.3s` 后仍保持 `407px`；FACEIT 三栏桌面同排且横向溢出 `0`，浏览器错误日志为空。
- `git diff --check`、`npm run build` 通过；未部署服务器，未动数据库。

## 2026-07-12 19:00 路径迁移与工作树修复

- 主仓库已迁移至 `D:\Agent files\codex\projects\ur-esports`，FACEIT SEA 工作树已迁移至 `D:\Agent files\codex\projects\worktrees\ur-esports\faceit-sea-task-board`。
- 迁移均执行复制、文件数与逐文件 SHA-256 校验后才删除旧目录；C 盘主目录快照、旧 D 盘工作树差异归档与 Git 修复前副本均保留在 D 盘项目目录或 `_rollback`。
- 已用 `feature/faceit-sea-task-board` 重建有效 Git worktree 并回填 13,822 个业务文件，逐文件 SHA-256 无差异；3011 与 5174 已从 D 盘工作树恢复并返回 HTTP 200。
