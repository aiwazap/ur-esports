# UR Esports 服务器安全加固记录（2026-07-15）

## 加固前的实测风险

| 问题 | 证据 |
|---|---|
| root 可密码登录，且正被爆破 | `permitrootlogin yes` + `passwordauthentication yes`；近 7 天 SSH 失败登录 **114 次，其中 92 次针对 root**；来源 `43.153.227.184`、`132.232.99.209`、`14.103.118.212` 等 |
| 无爆破防护 | **fail2ban 未安装** → 攻击者可无限次尝试，无人封禁 |
| 测试站 3001 公网直连 | ufw 开放 `3001/tcp ALLOW Anywhere`，实测 `curl http://124.220.64.8:3001/` 返回 **200** |
| nginx 零限流 | `limit_req`/`limit_conn` 配置数为 **0** → CC 攻击直达 Node |

## 已执行的加固（全部实测验证）

### 1. SSH 加固
- `PermitRootLogin: yes → no`（30 天内 root 成功登录 **0 次**，确认无人使用后才关）
- `PasswordAuthentication: yes → no`（仅允许密钥）
- `MaxAuthTries: 6 → 3`
- **关键坑**：腾讯云在 `/etc/ssh/sshd_config.d/50-cloud-init.conf` 里**强制覆盖** `PasswordAuthentication yes`。只改主配置无效，必须一并修改。以 `sshd -T` 的输出为最终真实值。
- **安全流程**：改前确认①密钥登录可用（5 把授权密钥）②免密 sudo 可用（关密码不影响提权）；改后用 `reload`（不断开现有连接）并**立即新开连接验证成功**才算完成。

### 2. fail2ban
- 已安装并 enable，`[sshd]` jail 启用（backend=systemd）
- 策略：**10 分钟内失败 3 次 → 封禁 1 小时**
- 白名单：`127.0.0.1/8 ::1 58.38.48.93`（华哥常用 IP，防误封）
- **`mode = aggressive`（关键，见下方坑）**

#### ⚠️ 重大坑：fail2ban「装了 ≠ 生效」，不实测发现不了

**现象**：安装后 `systemctl is-active` = active，`fail2ban-client status sshd` 一切正常，看起来完全没问题。
但同一时间实测：当天 **root 登录尝试 428 次**（单个 IP `82.157.142.56` 打了 **331 次**），而 fail2ban 显示：
```
Currently failed: 0    Total failed: 0
Currently banned: 0    Total banned: 0
```
**一个都没封。**

**根因**：fail2ban 默认的 sshd filter（`mode = normal`）匹配的是 `Failed password for ...` 这类日志。
但我们**禁用密码登录后**，攻击者根本走不到密码验证阶段，被挡在 preauth：
```
Connection closed by authenticating user root 82.157.142.56 port 55552 [preauth]
Connection reset by 82.157.142.56 port 54870 [preauth]
```
这些日志行 **normal 模式不匹配** → fail2ban 视而不见 → 加固越严，fail2ban 反而越瞎。

**修复**：`jail.local` 的 `[sshd]` 段加 `mode = aggressive`，重启后立即开始识别。

**验证（必须做）**：
```bash
sudo fail2ban-client status sshd
# 重启前: Total failed: 0   Total banned: 0     ← 假象
# 修复后: Total failed: 4   Total banned: 1
#         Banned IP list: 82.156.125.132        ← 真的封到人了
```

**教训**：`is-active` 显示 active **只能证明进程在跑，不能证明它在干活**。安全组件必须用「真实攻击是否被拦」来验收，不能看状态输出。

### 3. 移除测试环境 3001（华哥已离职，不再需要）
- pm2 `ur-esports-dev` 已 stop + delete + save
- ufw 移除 `3001/tcp` 规则；实测外网访问返回 **000（不可达）**
- 删除 `/home/ubuntu/ur-esports-dev`（589M）
- 备份脚本已适配：不再备份 test3001（原逻辑备份在 `_cfg_backup_20260715/backup_7day.sh.before_dev_removal`）
- 保险：`backups_7day/test3001/test3001_20260715_030001.tar.gz`（360M）**保留未删**，确认无需后可自行删除
- 确认无依赖：nginx 未引用 3001；crontab 里的 dev 条目早已停用（行首为 #）

### 4. nginx 限流（抗 CC）
`nginx.conf` 注入 zone：
```nginx
limit_req_zone $binary_remote_addr zone=general:10m rate=30r/s;
limit_req_zone $binary_remote_addr zone=api:10m     rate=10r/s;
limit_req_zone $binary_remote_addr zone=login:10m   rate=20r/m;
limit_conn_zone $binary_remote_addr zone=perip:10m;
limit_req_status 429;
limit_conn_status 429;
```
应用到站点：
| 位置 | 限速 | 并发 |
|---|---|---|
| `= /api/auth/login` | 20 次/分钟（burst 5） | 10 |
| `/api/` | 10 次/秒（burst 20） | 20 |
| `/vault` | 20 次/分钟（burst 5） | 10 |
| `/`（静态） | 30 次/秒（burst 50） | 30 |

**实测**：连打 30 次登录接口 → **25 次被 429 拦截**，5 次正常返回 401。等窗口恢复后正常登录/游客/读数据全部 200 ✓

## 加固后核对

- 网站首页 200 ✓ 总览 200 ✓ 档案页 401（口令保护正常）✓
- 郑蔼平登录 200（admin）✓ 游客进入 ✓ 读数据 200 ✓
- pm2：仅剩 `ur-esports` 一个进程 online ✓
- 磁盘：**63%**（删除测试环境释放 589M）

## 实战验证：加固当天的真实攻防数据

加固后数小时内的实测（不是理论，是真的有人在打）：

| 指标 | 数值 | 说明 |
|---|---:|---|
| root 登录尝试被拒 | **428 次** | `PermitRootLogin no` 全部挡下 |
| 密码登录被拒 | **47 次** | `PasswordAuthentication no` 全部挡下 |
| 最凶攻击源 `82.157.142.56` | **331 次** | 单 IP 持续爆破 |
| fail2ban 实际封禁 | **82.156.125.132** | 修 aggressive 后立刻抓到 |

**结论**：机器人现在即使找到 22 端口也是白敲——无密码可试、试 3 次即封 1 小时。

## 决策记录：为什么放弃「IP 白名单」和「改 SSH 端口」

### ❌ IP 白名单（限制只有华哥的 IP 能连 22）—— 放弃
调查发现三个硬伤，任一都会导致锁死：
1. **华哥使用 VPN** → 源 IP 会变成 VPN 出口（实测 `67.230.171.88` 今日登录 4 次，即 VPN 出口）
2. **无法证明主 IP 稳定**：日志只能追溯 3 天（系统日志已压至 7 天 + 服务重启），国内家庭宽带多为动态 IP
3. **`ubuntu` 有 5 把授权密钥**（`ur-esports-deploy`、`Administrator@Ultra-270Plus` ×2、`administrator@aiwazap-9800x3D`、`ubuntu@VM-0-8-ubuntu`）→ 多机多渠道在连，单 IP 限制会全废
4. 锁死后唯一救援通道是腾讯云控制台 VNC，华哥不熟 → 代价可能是重装系统

**结论：为防爆破把自己搞成重装，不值。**

### ⏸️ 改 SSH 端口（22 → 高位）—— 建议不做
- **收益**：让无脑扫 22 的机器人扫不到（日志清净）
- **成本**：任何写死 22 端口的工具（codex、`ur-esports-deploy` 部署密钥等）都要跟着改
- **权衡**：实测已证明——机器人就算敲到 22 也白敲（428 次全废 + fail2ban 封 IP）。改端口属**锦上添花非雪中送炭**，性价比不高
- 若将来仍要改：必须先盘清所有连 22 的工具，改完用新端口实测连通才算完成

## 现在能挡什么 / 不能挡什么

| 攻击类型 | 加固前 | 加固后 |
|---|---|---|
| SSH 暴力破解 | ❌ root 可爆破，7 天被打 92 次 | ✅ 禁 root、禁密码、3 次封 IP 1 小时 |
| 网站后台密码爆破 | ⚠️ 仅应用层 15 分钟 10 次 | ✅ 应用层 + nginx 20 次/分钟 双层 |
| CC 攻击（刷接口） | ❌ 直达 Node | ✅ nginx 层拦截，实测 429 |
| 测试站被当跳板 | ❌ 3001 裸奔公网 | ✅ 已彻底移除 |
| **真 DDoS（灌带宽）** | ❌ | ❌ **仍无法防御** |

**关于 DDoS 的实话**：DDoS 拼的是带宽，一台轻量服务器无论怎么配置都扛不住。唯一解法是买高防/CDN 让流量先经大管道过滤——属花钱的事，非配置能解决。上述加固能挡的是 CC（刷接口打死 Node），这个已经防住了。

## codex 复查后的三项收尾（2026-07-15）

codex 独立复查结论为 **CHANGES_REQUIRED**（核心加固有效，3 项必须收尾）。三项均已执行：

### 收尾 1：移出 nginx 中的冲突备份配置 —— **codex 抓到的真问题，是我的疏忽**
- **问题**：`/etc/nginx/sites-enabled/` 里同时存在 `ur-esports` 与 `ur-esports.bak_before_vault_20260714`（我 7/14 加 vault 时留下的），两者声明相同 `server_name` → `nginx -t` 报 **4 条 conflicting server name**。
- **危险性**：当前因主文件先加载，限流仍生效；但**文件排序或改名可能让旧配置抢先生效** → 限流与 vault 静默失效。
- **处置**：移出到 `_cfg_backup_20260715/nginx/`（未删除）。移前已确认移走的确为旧备份（`limit_req` 计数 0、无 `location /vault`），当前生效版含 4 处限流 + vault。
- **结果**：冲突警告 **4 → 0**，reload 成功。

### 收尾 2：fail2ban `maxretry` 3 → 5
- **codex 理由（成立）**：aggressive 模式合并更广的 SSH 异常模式；日志已出现华哥 VPN 地址 `67.230.171.88` 正常登录前的 `Connection reset ... [preauth]`。一次不封，但 **VPN 抖动 10 分钟内累计 3 次可能误封**，而 VPN 出口不在白名单。
- SSH 已禁 root、禁密码，3→5 不会重新引入认证风险。
- **生效值**：`maxretry=5`、`findtime=600`、`bantime=3600`、`mode=aggressive` 保留。
- **误封自救**：`sudo fail2ban-client set sshd unbanip <IP>`（需先能登录）；彻底锁死时用腾讯云控制台 VNC。

### 收尾 3：授权密钥去重 + 核实 deploy key
- **codex 更正了我的说法**：不是「5 把密钥」，实为 **5 行 / 4 把唯一密钥**。已实测指纹确认：
  ```
  ur-esports-deploy               SHA256:7eBEVNhXJNLXjjnslqGldM2F3BgYiUYr+HAfaWheO+w
  ubuntu@VM-0-8-ubuntu            SHA256:HePZEctShMtR7u7j3Q560LrrLqH4UYz6hLngRjQkqOU
  Administrator@Ultra-270Plus     SHA256:S3JP8n5BSMkzgSSQ1QN1Bda6aP1+HqaGcendyI2iAtE  ← 重复
  Administrator@Ultra-270Plus     SHA256:S3JP8n5BSMkzgSSQ1QN1Bda6aP1+HqaGcendyI2iAtE  ← 重复
  administrator@aiwazap-9800x3D   SHA256:P2Tj5kQwIoG/MSF3MxVK9J99n4pwiQZ7P3yFICA0wJg
  ```
- **处置**：备份原文件（`authorized_keys.bak_5lines`）后去重 → **5 行 → 4 行 / 4 把唯一指纹**。改后**立即用全新连接验证登录成功**。
- **`ur-esports-deploy` 已于同日撤销**（华哥确认）。撤销前核实：近 3 天**登录次数 0**、服务器上无脚本引用（仅 `__pycache__` 二进制误匹配）、**本机 `~/.ssh` 无对应私钥** → 无法证明仍在使用。
  - 处置：原文件备份 `authorized_keys.bak_before_deploykey_removal_4lines`；被撤销的公钥**单独存档** `revoked_ur-esports-deploy.pub`（99 字节），日后需要可原样加回。
  - **4 行 → 3 行**。撤销后**连续两次全新连接验证成功**（用的是 `S3JP8n...` = Ultra-270Plus，Claude 与 codex 共用的那把）。
  - 服务全部正常：PM2 online、nginx active、fail2ban active、chromium active；首页 200 / 总览 200 / 档案页 401。

**最终授权密钥清单（3 把）**
| 密钥 | 指纹 | 用途 |
|---|---|---|
| `ubuntu@VM-0-8-ubuntu` | `SHA256:HePZEctS...` | 服务器本机自用 |
| `Administrator@Ultra-270Plus` | `SHA256:S3JP8n5B...` | **Claude 与 codex 当前在用**（含华哥 VPN 出口） |
| `administrator@aiwazap-9800x3D` | `SHA256:P2Tj5kQw...` | 华哥另一台机器 |

### 我认同并采纳的 codex 判断
1. 不做 IP 白名单 —— 判断正确（动态家宽 + VPN 出口 + 多终端 = 高锁死风险）
2. 暂不改 SSH 端口 —— 判断正确（减噪声不替代认证安全，收益低于工具链断连风险）
3. 暂不启用 `[recidive]` —— 同意，先跑 7 天收数据；攻击者轮换云 IP 时收益有限
4. 腾讯云来源 IP **不能直接定性为横向入侵** —— 同意，只能确认在扫描；持续高频再向腾讯云 abuse 报告
5. nginx 阈值可用，不再收紧（避免页面并发误伤）
6. 暂不买高防 —— 同意，低流量内部站、无带宽攻击证据

### ⚠️ 测试的副作用（记录备查）
验收时连打 12 次登录接口，**把应用层登录配额烧光**（`loginLimiter`：15 分钟 10 次，按 IP 计），导致自己 15 分钟内无法登录。
**含义**：同一出口 IP 下，**我做限流测试会连带把华哥的登录配额一起用掉**。今后压测登录接口需避开华哥使用时段，或提前告知。

## 2026-07-15 晚：关闭游客访问，转为「仅授权账号可访问」

**起因**：华哥问"网站算绝对私密了吗"，并认为"外部访问不到"。**实测证伪**——用完全匿名的请求（无任何账号密码）调用 `/api/auth/guest` 拿到令牌后，成功读取选手数据（200）。**游客入口 = 对全世界敞开的只读门**，与"私密"完全相反。今天所有加固（禁 root/fail2ban/限流）防的是"撬锁"，不防"从敞开的正门走进来"——fail2ban 不会拦游客，因为他没在暴力破解，是合法进入。

华哥选择方案 A：**删除游客入口**。

### 执行
1. **后端（治本）**：删除 `routes/auth.js` 的 `POST /guest` 接口。
   - ⚠️ **中途纠正**：曾把历史令牌兜底写进 `routes/auth.js` 的 `router.use`，但那只管 `/api/auth/*`，历史令牌照样能打 `/api/players`。**真正的必经关卡是 `middleware/auth.js`**，已移至该处。
2. **middleware/auth.js**：由"guest 只读"改为**直接拒绝一切 guest 令牌**（含关闭前签发、仍在 12h 有效期内的历史令牌）。
3. **前端**：删除游客按钮、`guestLogin` 函数、分隔线；顺带移除"立即注册"入口（后端注册本就 403，前端不该再露）；清理未使用的 `Link` 导入。文案改为「本系统仅限授权账号访问」。
4. **密码**：`admin` 由弱密码 `admin123` 改为 `aZAP@1211`；`郑蔼平` 经校验已是该密码，未改动。改前备份数据库 `ur_esports.db.bak_before_pwchange_20260715`。

### 验证（匿名攻击面）
```
调用游客接口        → 404  ✅ 接口已不存在
无凭证读选手数据    → 401  ✅ 被拒
尝试注册新账号      → 403  ✅ 已关闭
真实历史游客令牌读  → 403  ✅ 被拒（用生产密钥签发的真令牌实测）
真实历史游客令牌写  → 403  ✅ 被拒
```
⚠️ **测试方法教训**：首次用**本地** `.env` 的 JWT_SECRET 伪造历史令牌 → 返回 401（签名无效），**测试无意义**。本地与生产 JWT_SECRET 不同，必须**在服务器上用生产密钥签发**才能真实验证。构建包也已确认 `游客访问`/`auth/guest`/`立即注册` 命中数均为 **0**。

### 授权账号仍正常
```
admin   → 200 ✅ 角色 admin，读数据 200（选手 24 条）
郑蔼平  → 200 ✅ 角色 admin
```

### 现在的私密性边界（诚实说明）
- ✅ **数据私密**：任何接口都需登录，只有 2 个账号
- ⚠️ **站点本身仍可被访问到**：`https://ur-esports.cn` 首页/登录页对公网返回 200，任何人都能看到"UR Esports 登录页"的存在，只是进不去
- 若要连"存在性"都隐藏，需整站 IP 白名单——但华哥用 VPN（出口 IP 会变），会把自己挡在外面，**不建议**

## 回滚

配置备份全在 `/home/ubuntu/_cfg_backup_20260715/`：
```bash
ssh ubuntu@124.220.64.8
B=/home/ubuntu/_cfg_backup_20260715

# 回滚 SSH（恢复密码登录与 root 登录）
sudo cp $B/ssh/sshd_config.bak /etc/ssh/sshd_config
sudo cp $B/ssh/sshd_config.d/50-cloud-init.conf /etc/ssh/sshd_config.d/
sudo sshd -t && sudo systemctl reload ssh

# 回滚 nginx 限流
sudo cp $B/nginx.conf.before_ratelimit /etc/nginx/nginx.conf
sudo cp $B/nginx-ur-esports.before_ratelimit /etc/nginx/sites-enabled/ur-esports
sudo nginx -t && sudo systemctl reload nginx

# 停用 fail2ban
sudo systemctl disable --now fail2ban

# 回滚 codex 复查后的三项收尾
sudo cp $B/nginx/ur-esports.bak_before_vault_20260714 /etc/nginx/sites-enabled/   # 恢复冲突备份配置（不建议）
sudo cp $B/jail.local.before_maxretry5 /etc/fail2ban/jail.local && sudo systemctl restart fail2ban  # maxretry 5→3
cp $B/authorized_keys.bak_5lines /home/ubuntu/.ssh/authorized_keys && chmod 600 /home/ubuntu/.ssh/authorized_keys  # 恢复 5 行（含重复）

# 恢复游客访问（若日后又想对外开放只读）
cp $B/auth.js.before_guest_removal /home/ubuntu/ur-esports/backend/middleware/auth.js
cp $B/routes-auth.js.before_guest_removal /home/ubuntu/ur-esports/backend/routes/auth.js
pm2 restart ur-esports --update-env
# 注意：前端按钮需重新构建部署才会出现

# 恢复被撤销的 ur-esports-deploy 密钥（若日后发现仍需要）
cat $B/revoked_ur-esports-deploy.pub >> /home/ubuntu/.ssh/authorized_keys
# 或整体回到撤销前的 4 行：
# cp $B/authorized_keys.bak_before_deploykey_removal_4lines /home/ubuntu/.ssh/authorized_keys && chmod 600 /home/ubuntu/.ssh/authorized_keys

# fail2ban 误封自己时的解封（需先能登录；彻底锁死则用腾讯云控制台 VNC）
sudo fail2ban-client set sshd unbanip <你的IP>

# 恢复测试环境（从备份）
cd /home/ubuntu && tar -xzf backups_7day/test3001/test3001_20260715_030001.tar.gz
sudo ufw allow 3001/tcp
cd ur-esports-dev/backend && pm2 start server.js --name ur-esports-dev && pm2 save
cp $B/backup_7day.sh.before_dev_removal /home/ubuntu/backup_7day.sh
```

## 建议的后续（未执行，需授权）

1. **改 SSH 端口**（22 → 高位端口）：能让绝大多数自动化扫描直接扫不到
2. **ufw 限制 SSH 来源 IP**：只允许你的固定 IP 连 22，最彻底
3. **上 CDN/高防**：唯一能对抗真 DDoS 的手段（花钱）
4. `/swapfile` 8G 使用量为 0，可评估缩减
