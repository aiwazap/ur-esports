# ur-esports(UR 赛训数据中心)— Claude 调试副本

## 项目定位
- UR 战队内部赛训系统 Web 版:`backend\`(Node/Express API)+ `frontend\`(前端)
- 本目录是 **Claude 侧调试副本**,2026-07-12 复制自权威主仓库 `D:\Agent files\codex\projects\ur-esports`(分支 main),复制时排除了 node_modules

## 每次会话先读
1. `项目记忆\01-项目规则与关键决策.md`(长期规则/红线)
2. `项目记忆\02-当前状态.md`(最近状态/待办)
3. `AGENTS.md` 是 Codex 侧规则,其中执行边界与红线在本副本同样适用

## 首次运行前(依赖未安装)
- `cd backend` → `npm install`
- `cd frontend` → `npm install`

## 红线(继承自主仓库规则,不可违反)
- 禁止对生产 3000 做任何写操作;测试验证走 3001;仅在华哥明确说"推生产"时才碰 3000
- 数据只允许 生产 → 本地 单向拉取,严禁本地数据反推生产
- 禁止把数据库、uploads、`.env`、密钥提交进 Git
- 改重要文件/配置前先备份,保留可回滚路径

## 双副本同步规则
- 权威主仓库在 codex 侧;本副本的改动如需回流主仓库,必须先向华哥确认,再整理成清晰的变更同步过去,防止两边分叉

## 协作方式
- 简体中文,列表模式,直接高效,不要客套话
- 华哥用文字描述需求,不太懂技术;先复述理解并确认,再动手
