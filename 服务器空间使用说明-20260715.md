# 服务器空间使用说明（2026-07-15）

> 回答一个问题：**UR-ESPORTS 才几百兆，为什么服务器占了几十个 G？**
> 一句话答案：**UR 项目只占 1G（4%），剩下 96% 是操作系统、AI 工具链、交换文件——都不是"我们的网站"。**

## 一、40G 的全景账（实测）

| 占用方 | 大小 | 占比 | 是什么 | 属于 UR 项目？ |
|---|---:|---:|---|---|
| `/swapfile` | **8.1G** | 20% | 交换文件（虚拟内存） | ❌ 系统 |
| `/root` | **5.3G** | 13% | AI 工具链（见下） | ❌ 别的工具 |
| `/usr` | **5.0G** | 13% | Linux 系统程序本体 | ❌ 系统 |
| `/home` | **2.6G** | 7% | 见第三节 | ⚠️ 部分是 |
| `/swap.img` | **2.0G** | 5% | 第二个交换文件 | ❌ 系统 |
| ext4 保留块 | **1.7G** | 4% | 文件系统预留（防写满死机） | ❌ 系统 |
| `/var` | **985M** | 2% | 系统日志、包管理 | ❌ 系统 |
| `/boot` `/tmp` 等 | ~300M | 1% | 启动与临时文件 | ❌ 系统 |
| **已用合计** | **25G** | **65%** | | |
| **可用** | **14G** | 35% | | |

## 二、真正的三个大头（占 15.1G，全都与网站无关）

### 1. 交换文件 10.1G（`/swapfile` 8G + `/swap.img` 2G）
- **这是硬盘上模拟的内存**。服务器物理内存只有 **1.9G**，内存不够时用硬盘顶。
- 实测：`/swap.img` 在用 **479M**；`/swapfile` 使用量 **0B**（8G 几乎没被用到）
- **它不是垃圾，不能当垃圾删**。但 8G 那个明显过剩，可评估缩减——属系统级改动，需单独授权。

### 2. `/root` 5.3G —— AI 工具链，不是 UR 的东西
| 明细 | 大小 | 说明 |
|---|---:|---|
| `.local/share/pnpm` | 2.3G | Node 包管理器的全局仓库 |
| `.openclaw` | 1.8G | OpenClaw 工具（extensions 1.3G + npm 478M） |
| `.cache/ms-playwright` | 625M | **Chromium 浏览器本体**（不是缓存，删了工具就跑不起来） |
| `.npm` | 379M | npm 缓存 |
| `.nvm` | 259M | Node 版本管理器 |

**这块是"几十个 G"的最大误解来源**：它看着在服务器上，但和 UR 网站零关系，是另一套 AI 工具的运行环境。

### 3. `/usr` 5.0G —— Linux 系统本体
`/usr/lib` 2.5G + `/usr/local` 904M + `/usr/share` 638M + `/usr/bin` 429M。装了系统就有，动不得。

## 三、`/home` 2.6G —— 这里才有 UR 的东西

| 目录 | 大小 | 说明 |
|---|---:|---|
| `backups_7day` | 739M | 每日备份（prod 190M×2 + test 360M） |
| `ur-esports-dev`（测试服） | 589M | 其中 uploads **285M**、node_modules 181M |
| **`ur-esports`（生产站）** | **365M** | **← 这才是真正的网站** |
| `backups` | 262M | 历史备份 + 数据库归档 |
| `_cfg_backup_20260715` | 22M | 今天的配置回滚点 |

### 生产站 365M 的构成
| 内容 | 大小 | 能不能删 |
|---|---:|---|
| `frontend/node_modules` | 128M | 可重建（`npm install`） |
| **`backend/uploads`** | **74M** | ❌ 选手照片，真实数据 |
| `backend/node_modules` | 43M | 可重建 |
| `frontend/dist` | 41M | 可重建（`npm run build`） |
| `.git` | 39M | 版本历史 |
| `frontend/public` | 38M | 源素材 |
| **`backend/data`** | **1.1M** | ❌ **主数据库——整个网站的核心，只有 1.1 兆** |

**关键认知：真正不可替代的数据只有 75M**（数据库 1.1M + 选手照片 74M）。其余 290M 全是可以一条命令重建的依赖和构建产物。

## 四、本次清理明细（2026-07-15）

| 动作 | 处理方式 | 大小 |
|---|---|---:|
| 生产 `backend/data` 里 17 份历史数据库副本 | **归档**到 `backups/db-archive-20260715/` | 15M |
| 旧构建副本 `dist.bak_before_guest_20260715` | 删除 | 41M |
| 源码目录散落的 9 个 `.bak_*` 文件 | **归档**到 `db-archive/code-bak/` | <1M |
| `ur-esports-backup-20260605` 全量目录（含 node_modules） | 先抢救出 6/5 数据库再删目录 | 199M |
| `dev3001_20260708` 测试包（已被 7/15 备份覆盖） | 删除 | 92M |
| `prod3000_before_migrate` 小库 | 归档 | 1M |

- **`backups` 目录：536M → 262M**
- **生产运行目录 `backend/data` 现在只剩主库一个文件**（此前 18 个）
- **零数据损失**：所有历史数据库都进了 `db-archive-20260715`（20 个文件 / 16M），含从 6/5 全量包里抢救出的历史点

**清理后核对**：主库 MD5 未变 ✓ `.env` MD5 未变 ✓ uploads 27 个 ✓ dist 85 个 ✓ vault 页在 ✓ PM2 双进程 online ✓ Chromium active ✓

## 五、为什么感觉"清了还是不够"

7/15 早上从 100% 清到 65%，释放 13.5G。但**磁盘不会回到很空**，因为剩下的 25G 里：
- **10.1G 是交换文件** —— 系统要用
- **5.0G 是 Linux 本身** —— 动不了
- **5.3G 是 AI 工具链** —— 不属于 UR
- **1.7G 是文件系统保留块** —— 防写满死机的安全垫

**能动的只有 `/home` 那 2.6G，而 UR 生产站本身只占 365M。**
换句话说：**就算把 UR 网站整个删光，也只能腾出 0.9%。**

## 六、后续可选项（均需单独授权）

1. **`/swapfile` 8G 使用量为 0** —— 评估缩减至 2-4G，可释放 4-6G。系统级改动，需谨慎（内存仅 1.9G）
2. **测试服 uploads 285M** —— 37 个测试文件，最大单个 49.5M。需先核对数据库引用，不能按大小删
3. **`/root` 5.3G 的 AI 工具链** —— 不是 UR 的东西，取决于那些工具还用不用（`ms-playwright` 是浏览器本体，OpenClaw 正在用，不可删）
4. **旧数据表技术债** —— 见 codex 审计报告第五节，属独立立项
